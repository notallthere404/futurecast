# Roadmap

- **Standalone CLI** — same inference path as the server, for
  one-shot classification of a file or stdin.

- **Semantic deduplication** — drop near-duplicate articles by
  embedding similarity, replacing today's URL-only dedupe.

- **First-party visualization** — server-rendered charts behind the
  same `View` API the dashboard consumes.
